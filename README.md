# CLI+Streamlit simple CSV profiler example project

This is a simple example for teaching python

```sh
pip install git+https://github.com/FarisHijazi/csv-profiler-dummy.git
```

```sh
uv run csv-profiler profile data/sample.csv --out-dir outputs --format both
```
